font-family: "<%= layer.font.name %>";
font-size: <%= layer.fontSize %>px;
text-anchor: middle;
inline-size: <%= layer.size.width %>;
text-align: <%= Web.cssAlign(layer.alignment) %>;
color: <%= layer.foregroundColor.hex %>;
overflow: hidden;
line-height: normal;
