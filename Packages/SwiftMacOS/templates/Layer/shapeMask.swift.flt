let <%= symbol.name %>Mask = CAShapeLayer()
<%= symbol.name %>Mask.name = "<%= symbol.name %>Mask"
<%= symbol.name %>Mask.path = <%= SwiftMacOS.code(snapshot.maskPath, layer: snapshot) %>
<%= symbol.name %>.mask = <%= symbol.name %>Mask
