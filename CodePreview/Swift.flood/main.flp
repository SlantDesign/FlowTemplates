import Swift

if selection.isEmpty
    process("empty.html.flt", into: outputPipe, indentation: 0)
end

for snapshot in selection
	var layer = snapshot.layer
    process("main.html.flt", into: outputPipe, indentation: 0)
end
